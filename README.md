# Texnika.kz

Texnika.kz — полнофункциональный маркетплейс спецтехники и операторов для Казахстана, вдохновленный Kolesa.kz и Avito Услуги.

## Текущий статус
- ✅ Монорепозиторий на pnpm workspaces
- ✅ Базовая структура приложений (web, api, admin)
- ✅ Общие пакеты (shared, ui, workers)
- ✅ Инфраструктурные заготовки (docker-compose, env)
- ✅ Prisma-схема, миграция и сиды для ключевых доменов
- ✅ **Phase B — API** (NestJS BFF: аутентификация, пользователи, дилеры, объявления, специалисты, медиа, чат, промо, поиск)
- ✅ **Phase C — Search** (OpenSearch индекс `listings`, ранжирование VIP/TOP/Highlight, синхронизация и reindex CLI)
- 🔄 Дальше: фронтенд UI, биллинг, очереди

## Структура
```
apps/
  web/      # Next.js 14 клиент
  api/      # NestJS BFF
  admin/    # Next.js административный интерфейс
packages/
  shared/   # Общие типы и доменные константы
  ui/       # Переиспользуемые UI-компоненты (shadcn/ui + tailwind)
  workers/  # BullMQ-воркеры и фоновые задачи
infrastructure/
  docker-compose.yml  # Локальная инфраструктура (Postgres, Redis, MinIO, OpenSearch, web, api, admin)
apps/api/prisma/
  schema.prisma       # Модель данных Prisma
  migrations/         # SQL-миграции
  seed.ts             # Сценарий сидирования демо-данных
```

## Скрипты верхнего уровня
- `pnpm dev` — параллельный запуск всех приложений (web, api, admin)
- `pnpm build` — сборка всех приложений
- `pnpm lint` — запуск линтеров
- `pnpm test` — все тесты
- `pnpm format` — форматирование кода

## Локальная разработка
1. Установите pnpm `npm install -g pnpm`
2. Скопируйте `.env.example` → `.env`
3. Запустите инфраструктуру: `docker compose -f infrastructure/docker-compose.yml up -d`
4. Установите зависимости: `pnpm install`
5. Примените миграции API (PostgreSQL должен быть запущен):
   ```bash
   pnpm --filter @texnika/api prisma:migrate
   pnpm --filter @texnika/api prisma:gen
   pnpm --filter @texnika/api prisma:seed
   ```
6. (Опционально) перестройте поисковый индекс после сидов: `pnpm --filter @texnika/api search:reindex`
7. Запустите сервисы разработки: `pnpm dev`

> ⚠️ Команды миграций/сидов не выполнялись автоматически — запустите их вручную в своей среде.

## Phase B — API
- [x] OTP-аутентификация, JWT в httpOnly cookie, роли и guards
- [x] Профили пользователей и дилеров, CRUD + статистика
- [x] Объявления спецтехники: CRUD, модерация, фильтры, фасеты
- [x] Каталог специалистов-операторов: фильтры, сортировки, CRUD
- [x] Медиа: presigned upload (MinIO) + привязка
- [x] Чат: WebSocket `/ws/chat` + REST-фиды
- [x] Продвижения (VIP/TOP/Highlight/Autobump) и пересчёт boost
- [x] Поисковой BFF `GET /api/search/listings` (OpenSearch + ранжирование)

Swagger-документация API доступна после запуска `@texnika/api` по адресу `http://localhost:3001/api/docs` (если порт не переопределён).

Подробнее про маршруты и payload'ы — в [`apps/api/README.md`](apps/api/README.md).

## OpenSearch и ранжирование

- Индекс `listings` синхронизируется сервисом `SearchSyncService` при создании, публикации, редактировании и продвижении объявлений.
- Ранжирование строится через `function_score` в OpenSearch: веса промо (VIP ×4, TOP ×2, Highlight ×1.2), `boostScore` и экспоненциальная свежесть (`freshnessScore`).
- Доступные сортировки: `relevance` (по умолчанию), `newest`, `price_asc`, `price_desc`, `year_desc`.
- Фасеты: категории (готово), остальные фасеты подключаются на фронте.
- Для полного пересоздания индекса: CLI `pnpm --filter @texnika/api search:reindex` или `POST /api/admin/search/reindex` (роль admin).


## Phase D — Web UI
- [x] Главная, каталоги и детальные страницы на Next.js 14 (SSR/ISR)
- [x] Фасетные фильтры, сортировки и пагинация через React Query + OpenSearch
- [x] Форма создания объявления с загрузкой медиа в MinIO и управлением промо
- [x] Личный кабинет: обзор, список объявлений, чаты
- [x] Компоненты маркетплейса (карточки техники/операторов, фильтры, загрузчик медиа)

### Скриншоты (placeholder)
| Экран | Файл |
| --- | --- |
| Главная | docs/screenshots/home.png (будет добавлено) |
| Каталог техники | docs/screenshots/listings.png (будет добавлено) |
| Каталог операторов | docs/screenshots/specialists.png (будет добавлено) |
| Личный кабинет | docs/screenshots/profile.png (будет добавлено) |
